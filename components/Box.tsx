import styled from 'styled-components';

const Box = styled.div`
  margin: auto;
  margin-top: 1rem;
  border: 1px solid #ddd;
  width: calc(90% - 2rem);
  max-width: 650px;
  padding: 1rem;
`;

export default Box;
